# Standard → Pro 라이선스 전환 절차

이 변경은 신규 판매 플랜을 **Free, Basic, Pro, Enterprise**의 네 단계로 바꾸고, 기존 `standard` 라이선스를 기능 손실 없이 `pro`로 승격합니다.

## 변경 내용

| 항목 | 처리 방식 |
|---|---|
| 신규 Standard 발급 | 관리자 발급 화면과 플랜 정의에서 제거 |
| 기존 Standard 고객 | 레거시 호환 로직으로 즉시 Pro 권한을 부여하고, DB 마이그레이션으로 `plan` 값을 `pro`로 영구 변경 |
| 기존 키·서버·기간·상태 | 변경하지 않음 |
| Pro 기능 | 기존 Standard의 보안·이벤트 기능과 기존 Pro의 파트너 기능을 모두 유지 |

## 안전한 배포 순서

1. 운영 MongoDB를 백업합니다. 복구 가능 여부와 백업 시점을 기록합니다.
2. 이 코드 변경을 모든 실행 인스턴스에 배포합니다. 배포 중 구버전 인스턴스가 남아 Standard 키를 발급하지 않게 하려면 짧은 점검 시간을 두거나, 모든 인스턴스가 새 버전으로 교체됐는지 확인합니다.
3. 배포 환경에서 아래 명령으로 **조회 전용 점검**을 실행합니다. 이 명령은 데이터를 변경하지 않습니다.

   ```bash
   npm run migrate:standard-to-pro
   ```

4. 출력된 Standard 라이선스 수와 백업 기준 건수를 대조합니다. Active, available, expired, revoked 상태의 라이선스가 모두 집계되는지 확인합니다.
5. 아래 명령으로 실제 승격을 실행합니다. `--apply`가 있을 때만 `standard` 값을 `pro`로 변경합니다.

   ```bash
   npm run migrate:standard-to-pro -- --apply
   ```

6. 출력에서 `남은 Standard: 0개`를 확인합니다. 라이선스 관리자 화면에서 Standard 발급·필터 옵션이 사라졌는지, 표본 기존 고객의 플랜·만료일·서버 ID가 정상인지 확인합니다.

## 롤백 원칙

마이그레이션 이전 백업이 가장 안전한 롤백 수단입니다. 적용 직후 문제가 발견되고 다른 플랜 데이터에 변경이 없다면, 백업 검증 후 `plan: "pro"`로 바뀐 **대상 라이선스만** `standard`로 되돌릴 수 있습니다. 단, 전환 후 신규 Pro 라이선스까지 함께 되돌리면 안 되므로, 운영 환경에서 별도 기록이나 백업 기준 없이 단순 일괄 역변환은 수행하지 마세요.

## 마이그레이션 후 정리

전환이 안정화되어 DB에 `standard` 라이선스가 없다는 사실을 재확인한 뒤에만, `src/config/plans.js`와 `src/database/models/license.js`의 레거시 호환 코드를 다음 릴리스에서 제거할 수 있습니다. 이 정리는 즉시 필요하지 않으며, 현재 코드는 이전 고객 권한이 Free로 축소되는 위험을 방지하기 위해 호환성을 유지합니다.
